This file should not be filtered, meaning that ${project.version} should still be ${project.version} (dollar sign,
brace, project dot version, brace).

The 'nf' extension is important because it's the one used in the additional list of non filtered extensions.